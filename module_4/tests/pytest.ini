[pytest]
markers =
    web: web route tests
    buttons: button and busy-state behavior tests
    analysis: analysis rendering and formatting tests
    db: database write and query tests
    integration: end-to-end or multi-component tests

addopts = -m "web or buttons or analysis or db or integration"